# Get label of a scale

Get label of a scale

## Usage

``` r
get_label(data, scale, label)
```

## Arguments

- data:

  A data frame with dic information

- scale:

  A scale definition (e.g. scale = subscale == "Int").

- label:

  Name of label attribute

## Value

Labels for a given scale
