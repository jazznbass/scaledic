# Convert a data frame to a dictionary-like structure

Converts each column of the input data frame into a 'dic' object,
preserving any existing labels as item labels.

## Usage

``` r
as_dic(x)
```

## Arguments

- x:

  A data frame to be converted.

## Value

A data frame with each column converted to a 'dic' object.
